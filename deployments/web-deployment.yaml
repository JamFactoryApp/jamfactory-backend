apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: jamfactory
  name: jamfactory-web
  labels:
    name: jamfactory-web
spec:
  replicas: 1
  selector:
    matchLabels:
      name: jamfactory-web
  template:
    metadata:
      labels:
        name: jamfactory-web
    spec:
      restartPolicy: Always
      imagePullSecrets:
        - name: regcred
      containers:
        - image: registry.jole.dev/jamfactory/jamfactory/jamfactory-web:latest
          name: jamfactory-web
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
